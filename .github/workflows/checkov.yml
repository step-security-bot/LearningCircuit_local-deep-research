name: Checkov IaC Security Scan

on:
  push:
    branches: [main, dev, develop]
    paths:
      - 'Dockerfile'
      - 'docker-compose*.yml'
      - '.github/workflows/*.yml'
  pull_request:
    branches: [main, dev, develop]
    paths:
      - 'Dockerfile'
      - 'docker-compose*.yml'
      - '.github/workflows/*.yml'
  workflow_dispatch:

permissions: {}  # Minimal top-level for OSSF Scorecard Token-Permissions

jobs:
  checkov:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
        with:
          egress-policy: audit

      - uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0

      - name: Set up Python
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548 # v6
        with:
          python-version: '3.11'

      - name: Install Checkov
        run: |
          pip install checkov

      - name: Run Checkov on Dockerfiles
        id: checkov-docker
        run: |
          echo "=== Scanning Dockerfiles ==="
          checkov --framework dockerfile \
            --file Dockerfile \
            --output cli \
            --output sarif \
            --output-file-path console,checkov-docker.sarif \
            --soft-fail || echo "DOCKER_ISSUES=true" >> "$GITHUB_ENV"

      - name: Run Checkov on docker-compose files
        id: checkov-compose
        run: |
          echo "=== Scanning docker-compose files ==="
          checkov --framework docker_compose \
            --file docker-compose.yml \
            --file docker-compose.gpu.override.yml \
            --file docker-compose.tts.yml \
            --file docker-compose.unraid.yml \
            --output cli \
            --output sarif \
            --output-file-path console,checkov-compose.sarif \
            --soft-fail || echo "COMPOSE_ISSUES=true" >> "$GITHUB_ENV"

      - name: Run Checkov on GitHub Actions workflows
        id: checkov-workflows
        run: |
          echo "=== Scanning GitHub Actions workflows ==="
          checkov --framework github_actions \
            --directory .github/workflows \
            --output cli \
            --output sarif \
            --output-file-path console,checkov-workflows.sarif \
            --soft-fail || echo "WORKFLOW_ISSUES=true" >> "$GITHUB_ENV"

      - name: Upload Dockerfile SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@32f7c15247fa6542dc765bd40f6a15dc2caeed89 # v4.31.2
        with:
          sarif_file: checkov-docker.sarif
          category: checkov-dockerfile
        continue-on-error: true

      - name: Upload docker-compose SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@32f7c15247fa6542dc765bd40f6a15dc2caeed89 # v4.31.2
        with:
          sarif_file: checkov-compose.sarif
          category: checkov-compose
        continue-on-error: true

      - name: Upload GitHub Actions SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@32f7c15247fa6542dc765bd40f6a15dc2caeed89 # v4.31.2
        with:
          sarif_file: checkov-workflows.sarif
          category: checkov-workflows
        continue-on-error: true

      - name: Summary
        if: always()
        run: |
          echo "=== Checkov IaC Security Scan Summary ==="
          if [[ "$DOCKER_ISSUES" == "true" || "$COMPOSE_ISSUES" == "true" || "$WORKFLOW_ISSUES" == "true" ]]; then
            echo "⚠️  Security issues found (see details above)"
            echo "📊 SARIF results uploaded to GitHub Security tab"
            echo "Note: Using --soft-fail to not block CI"
          else
            echo "✅ No security issues found"
          fi
