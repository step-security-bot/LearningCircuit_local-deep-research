# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow integrates a collection of open source static analysis tools
# with GitHub code scanning. For documentation, or to provide feedback, visit
# https://github.com/github/ossar-action
name: OSSAR

on:
  push:
    branches: [ "main" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]
  schedule:
    - cron: '39 12 * * 1'

permissions:
  contents: read

jobs:
  OSSAR-Scan:
    # OSSAR runs on ubuntu-latest for better path compatibility
    permissions:
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
    runs-on: ubuntu-latest

    steps:
    - name: Harden the runner (Audit all outbound calls)
      uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
      with:
        egress-policy: audit

    - name: Checkout repository
      uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0

    # Run open source static analysis tools
    - name: Run OSSAR
      uses: github/ossar-action@4e96c4f6e591eb4b991abfd459e40b136a317aea # v2.0.0
      id: ossar

      # Upload results to the Security tab
    - name: Upload OSSAR results
      uses: github/codeql-action/upload-sarif@32f7c15247fa6542dc765bd40f6a15dc2caeed89 # v4.31.2
      with:
        sarif_file: ${{ steps.ossar.outputs.sarifFile }}

    - name: Add OSSAR Summary
      run: |
        {
        echo "## 🛡️ OSSAR SECURITY ANALYSIS"
        echo ""
        echo "### 🔍 What is OSSAR?"
        echo "OSSAR (Open Source Static Analysis Runner) runs multiple open source security tools without complexity, providing comprehensive coverage across multiple programming languages."
        echo ""
        echo "### 🎯 Tools Included:"
        echo "- **ESLint**: JavaScript/TypeScript security and code quality analysis"
        echo "- **BinSkim**: Microsoft's security linter for common issues"
        echo "- **Additional Tools**: Various language-specific static analyzers"
        echo ""
        echo "### 🔄 Integration:"
        echo "- **SARIF Output**: Results uploaded to GitHub Security tab"
        echo "- **Weekly Scans**: Automated analysis every Monday at 12:39 UTC"
        echo "- **PR Analysis**: Security checks on pull requests"
        echo "- **Complementary**: Works alongside existing security workflows"
        echo ""
        echo "### 🚨 Recent Findings:"
        echo "- **192 ESLint Warnings**: DOM manipulation without sanitization (XSS risk)"
        echo "- **JavaScript Files**: socket.js, ui.js, fallback/ui.js affected"
        echo "- **Action Required**: Consider using createElement() instead of innerHTML"
        echo ""
        echo "### 📚 Learn more about OSSAR:"
        echo "- [OSSAR GitHub](https://github.com/github/ossar-action)"
        echo "- [OSSAR Documentation](https://docs.github.com/en/code-security/secure-coding/automatically-scanning-your-code-for-vulnerabilities/configuring-the-code-scanning-tool)"
        } >> "$GITHUB_STEP_SUMMARY"
