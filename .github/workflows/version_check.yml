name: Version Check

on:
  pull_request:
    paths:
      - 'src/**'
      - 'pdm.lock'  # Dependency updates (security fixes)
      - 'pyproject.toml'  # Project config/dependency changes
      - 'package.json'  # Frontend dependency changes
      - 'vite.config.js'  # Frontend configuration changes
      - 'package-lock.json'  # Frontend locked dependencies
      - '.github/workflows/version_check.yml'  # Self-updates to version bump logic
      - 'scripts/ollama_entrypoint.sh'  # Critical scripts
      - 'Dockerfile'  # Main Dockerfile
    types: [ opened, synchronize, reopened ]
    branches:
      - 'main'
  workflow_dispatch:  # Manual trigger for edge cases

permissions:
  contents: read  # Restrict top-level, job escalates as needed
  pull-requests: read

jobs:
  version-check:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required for pushing version bump
      pull-requests: write  # Required for PR comments
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@df199fb7be9f65074067a9eb93f12bb4c5547cf2 # v2.13.3
        with:
          egress-policy: audit

      - name: Checkout code
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3 # v6.0.0
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}

      - name: Checkout PR branch
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          git checkout "${HEAD_REF}"

      - name: Set up Python
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548 # v6
        with:
          python-version: '3.12'

      - name: Check and auto-bump version
        if: "!contains(github.event.head_commit.message, 'chore: auto-bump version')"
        env:
          HEAD_REF: ${{ github.head_ref }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          if ! git diff "origin/${BASE_REF}..HEAD" -G"__version__" | grep -E '\+.*__version__.*='; then
            echo "Version not updated. Auto-bumping patch version..."

            # Get current version
            current_version=$(grep -oP '(?<=__version__ = ")[^"]*' src/local_deep_research/__version__.py)
            echo "Current version: $current_version"

            # Split version into parts
            IFS='.' read -ra VERSION_PARTS <<< "$current_version"
            major=${VERSION_PARTS[0]}
            minor=${VERSION_PARTS[1]}
            patch=${VERSION_PARTS[2]}

            # Increment patch version
            new_patch=$((patch + 1))
            new_version="$major.$minor.$new_patch"
            echo "New version: $new_version"

            # Update version file
            sed -i "s/__version__ = \"$current_version\"/__version__ = \"$new_version\"/" src/local_deep_research/__version__.py

            # Configure git
            git config --local user.email "action@github.com"
            git config --local user.name "GitHub Action"

            # Commit the version bump
            git add src/local_deep_research/__version__.py
            git commit -m "chore: auto-bump version to $new_version"

            # Push to the PR branch
            git push origin "${HEAD_REF}"

            echo "Version automatically bumped to $new_version"
          else
            echo "Version has been updated manually. No auto-bump needed."
          fi
